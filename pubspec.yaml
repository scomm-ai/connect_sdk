name: scommconnector
description: SComm Connector Flutter package
version: 1.0.0
publish_to: none
repository: git@github.com:dev10m-adv/scommconnector_flutter.git

environment:
  sdk: ">=3.3.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  protobuf: ^6.0.0
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^10.0.0
  fixnum: ^1.1.1
  get_it: ^9.2.0
  grpc: ^5.1.0
  signals: ^7.1.0
  equatable: ^2.0.8
  # flutter_webrtc: ^1.4.1
  flutter_webrtc:
    git:
      url: https://github.com/advforks/flutter-webrtc.git
      ref: main

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
